**** 2018.06.12 ****
Предыдущий файл - /home/alex/Documents/PHP5.6/term-180529(php);
После неудачных попыток запустить сайт Ганюшкина /ucpa
пришли вместе с ним к выводу,
что будем делать все по-новой на RoR;

*/- 1
Буду делать на новейшей стабильной версии Ruby + самая новая для него стабильная версия Rails;
Для этого установлю их здесь на Сервере - см.srv-osvita1-180612(rvm);


*/- 1.1
 Сначала проверяю, что установлено - п.1 - 6;

*/- 1.2
Заодно в п.6 внес ПРАВИЛЬНЫЙ ПУТЬ для rvm (что раньше постоянно выводило ошибку);

*/- 1.3
В п.7-10
устанавливаю новейшую версию 
ruby 2.4.1p111 (2017-03-22 revision 58053) [x86_64-linux]
и
Rails 5.2.0

*/- 1.4
В процессе этого
- проверяю, что установка других зависимостей для rvm не нужна - п.9;
- убеждаюсь, что RubyGems установлен - п.10.1;
- создаю гемсет для работы с Проектом /osvita:
	alex@www:~$ rvm use 2.4.1@rb241rl520 --create --default

**** Т.о., мною создана СРЕДА для разработки Проекта /osvita;
Теперь нужно запустить СОЗДАНИЕ ПРОЕКТА ...
- с БД Postgres;
- и без создания файлов тестирования по-умолчанию;
- и, пожалуй, без sksfford, поскольку он создает слишком много лишних файлов;

**** 2018.06.14 ****
Дома на НБ я повторил то, что сделал на Сервере по п.1.1-1.3 выше,
причем, версии и Ruby и Rails оказались в точноти такие же!!!!
- см./home/alex/Documents/pare_files/home/osvita/hm-osvita1-180612(xron)_at180613,
	и hm-osvita1-180612(rvm)_at180613;
	
Далее, на НБ я сгенерировал
rails new osvita --skip-test-unit --database=postgresql,
довел его до стартовой страницы Rails
и запушил!!!!

*/- 2
Теперь первым делом сгружу из Гитхаба запушенную ветку;
Все действия по запуску этой ветки на Сервере
и листинг терминала этого процесса 
изложены в файле srv-osvita3-180614(pull-start);

*/- 3
Двигаюсь дальше - заменяю дефолтную страницу в сгенерированном Проекте на "свою" "домашнюю"
- см. /home/alex/Documents/my_projects/osvita/srv-osvita4-180614(home-page);

*/- 3.1
Сначала подключаю Bootstrap
и делаю шаблоны схемы - п.1-4;
При этом кое-что мне осталост непрнятным (см.п.3),
но оно работает, поэтому в детали вникать не буду.
Если что-то вылезет - то это в подключении стилей Bootstraop, тогда буду копаться;

*/- 3.2
Задем заменяю дефолтную страницу Rails на "мою" стартовую страницу - п.5-7;
Коммит, пуш - п.8

**** 2018.06.19 ****
*/- 4
Регистрация пользователей;
Из ЮТ узнал о Devise;

*/- 4.1
Предварительно изучаю документацию - см.папку /home/alex/Documents/Rails/Devise;

*/- 4.2
Делаю установку Devise - см.srv-osvita5-180619(devise),
п.
**** НАЧАЛО  ****

**** КОНЕЦ  ****



**** НАЧАЛО srv-osvita5-180619(devise) ****
Здесь буду производить инсталляцию Devise = регистрацаия пользователей;
*/- 1.1
Делаю новую ветку:
alex@www:~/rails_projects/osvita$ git checkout -b devise

*/- 1.2
Раскомментирую в Gemfile строку ... 
(она была сразу после генерации; Раньше ее не было и она появилась только в Rails 5;
Я ее закоментировал, чтобы, в случае ошибок при запуске, было меньше вариантов;
К установке Devise это не имеет отношения)

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

*/- 2.1 - 2.6 Установка Devise:
gem 'devise'
bundle install
rails generate devise:install
config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }
../config/initializers/devise.rb
root 'welcome#index'

*/- 2.7
Перенаправление из этой точки на профиль пользователя
get 'persons/profile', as: 'user_root'
  пока не делаю

*/- 2.8 
Добавляю классы к flesh;

*/- 2.9 (шаг 3.4 по [2])
Здесь упоминается Heroku, но в случае Rails 3.2;
Поскольку у меня Rails 5, то я надеюсь, что для него уже все заточено, и эту директиву указывать не нужно;

*/- 2.10 (шаг 3.5 по [2])
По [2] теперь извлекаются вьюхи,
но пока я не планирую их менять, поэтому шаг 3.5 пропускаю;

*/- 2.11 - 2.17
Генерация модели User,
корректировка модели,
миграция

*/- 3.1 - 3.7
Добавляю кнопки "Вход / Регистрация" в Главное меню,
приделываю к ним маршруты,
удаляю ненужные кнопки из Главного меню,

*/- 3.8
Выполняю "Регистрацию" себя ... неудачно:
oo.gaydar@gmail.com

*/- 4.1
**//- 5 Выгружаю вьюхи devise:

*/- 4.2
Меняю заголовки:
в 
../osvita/app/views/devise/sessions/new.html.erb
'Log in' на 'Вход';

в 
../osvita/app/views/devise/registrations/new.html.erb
'Sign up' на 'Регистрация';

*/- 5 коммит, пуш
**** КОНЕЦ srv-osvita5-180619(devise) ****

**** НАЧАЛО srv-osvita6-180620(devise2) ****
*/- 1.5 -1.8 
Создаю "роль Postgres" = 'osvita',

*/- 2.3
Добавляю отладочную информацию:
 в ../osvita/app/views/layouts/application.html.erb
<%= debug(params) if Rails.env.development? %>

*/- 3.1 - 3.4
Добавляю перехватчик писем
  gem "letter_opener"

*/- 4 - 5.4
Добавляю flash,

*/- 5.5 коммит, пуш
   d289c92..2bc170b  master -> master

*/- 5.6
Возвращаюсь на ветку ... и там (до конца файла) "бьюсь" над почтой (нет письма для подтвержления)

**** КОНЕЦ srv-osvita6-180620(devise2) ****

